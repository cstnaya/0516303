dist: xenial

language: java
jdk:
  - oraclejdk11

install:
  - mvn -Dmaven.javadoc.skip=true -B -V  # install -Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8 -DskipTests=true 
  - echo "install finished"
  
script:
  - mvn test -B #-Dmaven.compiler.target=1.8 -Dmaven.compiler.source=1.8
  - echo "script finished"

